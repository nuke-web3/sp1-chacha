[package]
name = "chacha-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
chacha20.workspace = true
rand = { version = "0.9", default-features = false, optional = true, features = [
  "os_rng",
] }

[features]
default = ["std"]
std = ["rand/os_rng"]
